-- generated by hosc1 from examples/se/in/iter1.hs

data List a = Nil  | Cons a (List a);

(letrec
  g=(\p4->
    case  p4  of {
      Nil  -> Nil;
      Cons w1 v2 -> (letrec h=(\r4-> case  r4  of { Nil  -> (g v2); Cons t u2 -> (Cons (f t) (h u2)); }) in (h w1));
    })
in
  (g xs))
