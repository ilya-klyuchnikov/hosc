-- generated by hosc1 from examples/se/in/01b_rev.hl0

data List a = Nil  | Cons a (List a);

(letrec h=(\x4-> case  x4  of { Cons x2 s2 -> (Cons x2 (h s2)); Nil  -> (Cons v2 Nil); })
in
  (h
    (letrec
      f=(\s3->
        case  s3  of {
          Nil  -> Nil;
          Cons w3 w ->
            (letrec g=(\t3-> case  t3  of { Cons p1 t1 -> (Cons p1 (g t1)); Nil  -> (Cons w3 Nil); }) in (g (f w)));
        })
    in
      (f v3))))
