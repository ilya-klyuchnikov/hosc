number :: Z | S number;
list $a :: Nil  | Cons $a (list $a);
(letrec
  f=%y1 { case y1 of {
    Nil : (letrec g=%z1 { case z1 of { Nil : z; Cons r p : (Cons r (g p)); }} in (g y));
    Cons u s : (Cons u (f s));
  }
  }
in
  (f x))