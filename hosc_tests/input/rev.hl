list $a :: Nil | Cons $a list $a;

rev     :: list $a -> list $a;

app     :: list $a -> list $a;

rev = %xs {
  case xs of {
    Nil : Nil;
    Cons z  zs : Cons (app (rev zs)) z;
  }
}

app = %xs {
  %ys {
    case xs of {
      Nil : ys;
      Cons z zs : Cons z (app zs xs);
    }
  }
}