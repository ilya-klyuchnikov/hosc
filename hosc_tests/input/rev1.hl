list $a :: Nil | Cons $a list $a;
boolean :: True | False;

rev     :: list $a -> list $a;

app     :: list $a -> list $a -> list $a;


app = %xs {
  %ys {
    case xs of {
      Nil : ys;
      Cons z zs : Cons z (app zs xs);
    }
  }
}

rev = %xs {
  app (rev xs) (Cons (True) Nil)
}

