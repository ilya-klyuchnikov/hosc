list $a :: Nil | Cons $a (list $a);
boolean :: True | False;

rev c
  where

app = %xs {
  %ys {
    case xs of {
      Nil : ys;
      Cons z zs : Cons z (app zs xs);
    }
  }
}

rev = %xs {
  app (rev xs) xs
}

